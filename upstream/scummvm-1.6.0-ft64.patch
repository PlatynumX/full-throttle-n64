diff --git a/engines/scumm/insane/insane.cpp b/engines/scumm/insane/insane.cpp
index 44528e5..2417396 100644
--- a/engines/scumm/insane/insane.cpp
+++ b/engines/scumm/insane/insane.cpp
@@ -862,6 +862,7 @@ void Insane::queueSceneSwitch(int32 sceneId, byte *fluPtr, const char *filename,
 void Insane::smush_rewindCurrentSan(int arg_0, int arg_4, int arg_8) {
 	debugC(DEBUG_INSANE, "smush_rewindCurrentSan(%d, %d, %d)", arg_0, arg_4, arg_8);
 	_smush_setupsan2 = arg_0;
+	_player->setCurVideoFlags(_smush_setupsan2);
 
 	smush_setupSanFile(0, 0, 0);
 	_smush_isSanFileSetup = 1;
@@ -1415,6 +1416,7 @@ int32 Insane::smush_setupSanWithFlu(const char *filename, int32 setupsan2, int32
 		tmp += 8;
 
 	_smush_setupsan2 = setupsan2;
+	_player->setCurVideoFlags(_smush_setupsan2);
 
 	if (tmp[2] <= 1) {
 		/* 0x300 -- palette, 0x8 -- header */
@@ -1446,6 +1448,7 @@ void Insane::smush_setupSanFromStart(const char *filename, int32 setupsan2, int3
 	debugC(DEBUG_INSANE, "Insane::smush_setupFromStart(%s)", filename);
 	_smush_setupsan1 = setupsan1;
 	_smush_setupsan2 = setupsan2;
+	_player->setCurVideoFlags(_smush_setupsan2);
 	smush_setupSanFile(filename, 0, 0);
 	_smush_isSanFileSetup = 1;
 	smush_setFrameSteps(step1, step2);
diff --git a/engines/scumm/smush/smush_player.cpp b/engines/scumm/smush/smush_player.cpp
index ce098f0..0649b84 100644
--- a/engines/scumm/smush/smush_player.cpp
+++ b/engines/scumm/smush/smush_player.cpp
@@ -236,6 +236,7 @@ SmushPlayer::SmushPlayer(ScummEngine_v7 *scumm) {
 	_insanity = false;
 	_middleAudio = false;
 	_skipPalette = false;
+	_curVideoFlags = 0;
 	_IACTstream = NULL;
 	_smixer = _vm->_smixer;
 	_paused = false;
@@ -907,15 +908,28 @@ void SmushPlayer::handleAnimHeader(int32 subSize, Common::SeekableReadStream &b)
 	debugC(DEBUG_SMUSH, "SmushPlayer::handleAnimHeader()");
 	assert(subSize >= 0x300 + 6);
 
-	/* _version = */ b.readUint16LE();
+	const byte headerMajorVersion = b.readByte();
+	const byte headerMinorVersion = b.readByte();
 	_nbframes = b.readUint16LE();
 	b.readUint16LE();
 
-	if (_skipPalette)
-		return;
+	if (_skipPalette) {
+		b.skip(0x300);
+	} else {
+		readPalette(_pal, b);
+		setDirtyColors(0, 255);
+	}
 
-	readPalette(_pal, b);
-	setDirtyColors(0, 255);
+	// Full Throttle SMUSH files can carry a per-video frame-rate override.
+	// This is a no-allocation 1.6.0 adaptation of upstream 9e7e6a0.
+	if (_vm->_game.id == GID_FT && headerMajorVersion > 1 && subSize >= 0x308) {
+		const uint16 speed = b.readUint16LE();
+		if ((_curVideoFlags & 8) == 0 && speed != 0) {
+			debug(5, "SmushPlayer::handleAnimHeader(): header version %d.%d, video speed override %d fps (cur speed %d)",
+				headerMajorVersion, headerMinorVersion, speed, _speed);
+			_speed = speed;
+		}
+	}
 }
 
 void SmushPlayer::setupAnim(const char *file) {
@@ -926,6 +940,10 @@ void SmushPlayer::setupAnim(const char *file) {
 		readString(file);
 }
 
+void SmushPlayer::setCurVideoFlags(int16 flags) {
+	_curVideoFlags = flags;
+}
+
 SmushFont *SmushPlayer::getFont(int font) {
 	char file_font[11];
 
diff --git a/engines/scumm/smush/smush_player.h b/engines/scumm/smush/smush_player.h
index 5a20240..a7707c7 100644
--- a/engines/scumm/smush/smush_player.h
+++ b/engines/scumm/smush/smush_player.h
@@ -81,6 +81,7 @@ private:
 	bool _insanity;
 	bool _middleAudio;
 	bool _skipPalette;
+	int16 _curVideoFlags;
 
 public:
 	SmushPlayer(ScummEngine_v7 *scumm);
@@ -92,6 +93,7 @@ public:
 	void play(const char *filename, int32 speed, int32 offset = 0, int32 startFrame = 0);
 	void release();
 	void warpMouse(int x, int y, int buttons);
+	void setCurVideoFlags(int16 flags);
 
 protected:
 	int _width, _height;
diff --git a/gui/module.mk b/gui/module.mk
index bda3c88..466273c 100644
--- a/gui/module.mk
+++ b/gui/module.mk
@@ -13,7 +13,6 @@ MODULE_OBJS := \
 	message.o \
 	object.o \
 	options.o \
-	predictivedialog.o \
 	saveload.o \
 	saveload-dialog.o \
 	themebrowser.o \
